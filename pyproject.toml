[project]
name = "chord-keeper"
version = "0.1.0"
description = ""
authors = [
    {name = "Giovanni",email = "giovcb@hotmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyautogui (>=0.9.54,<0.10.0)",
    "keyboard (>=0.13.5,<0.14.0)",
    "customtkinter (>=6.0.0,<7.0.0)"
]

[tool.poetry]
packages = [{include = "core_player", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
